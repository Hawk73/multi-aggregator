= bootstrap_form_for(@request, url: add_request_provider_path(@provider), inline_errors: true) do |f|
  = f.text_area :text, size: "20x5", placeholder: "Create a request", hide_label: true
  = f.submit "Add request"
